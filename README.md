# 🧩 JSFetchData

A simple JavaScript module to **fetch data from APIs** and display it dynamically in the browser using **Fetch API**. Designed for beginners to learn async data handling and DOM manipulation.

## 🚀 Features

- Lightweight, dependency-free JavaScript module  
- Fetches JSON data from any REST API  
- Provides clean success and error handling  
- Easily render fetched data into HTML elements  

## 📁 Repository Structure